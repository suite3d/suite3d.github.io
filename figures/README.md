Drop paper figures (PNG or SVG) here, then copy `figures.example.json` to
`figures.json` and describe them. They appear on the landing page in order.
Until `figures.json` exists the landing page shows a placeholder notice.
